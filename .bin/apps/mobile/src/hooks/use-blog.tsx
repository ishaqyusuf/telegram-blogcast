import { ItemProps } from "@/components/home-feed/home-feed-post-card";
import { useAudioStore } from "@/store/audio-store";

export function useBlog(item: ItemProps) {
  const store = useAudioStore();
}
