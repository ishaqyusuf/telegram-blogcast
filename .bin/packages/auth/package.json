{
  "name": "@acme/auth",
  "version": "1.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./utils": "./src/utils.ts"
  },
  "prisma": {
    "schema": "./src/schema"
  },
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "db:generate": "dotenv -e ../../.env -- bunx @better-auth/cli",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@acme/db": "workspace:*",
    "@acme/utils": "workspace:*",
    "@next-auth/prisma-adapter": "^1.0.7",
    "better-auth": "^1.4.3",
    "next-auth": "^4.24.11"
  },
  "devDependencies": {
    "@better-auth/cli": "1.3.7",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "typescript": "^5.9.3"
  }
}
