# Task: Annotation Protection And Account Isolation

## Status
Done

## Priority
Medium

## Created Date
2026-09-08

## Last Updated
2026-09-09

## Global Ticket
- Ticket Position: 6/8

## Source Context
[Mobile Book Local Cache plan](../plans/2026-09-08-feature-mobile-book-local-cache.md). User requests Android/media/com.alghurobaa.podcast/Books/book-{databaseBookId}/, mobile implementation, and progress tracking.

## Plan Status
In Progress

## Plan File
[Plan](../plans/2026-09-08-feature-mobile-book-local-cache.md)

## Implementation Progress
- Completion: 100%
- Current Checklist: 4/4
- Blockers: None for this ticket; overall preview acceptance remains in Ticket 8.

## Implementation Checklist
- [x] Keep highlights, comments, drafts, and account identifiers outside portable content files.
- [x] Scope annotation, draft, bookmark, and pending-sync state without losing legacy user data.
- [x] Verify retry-safe annotation synchronization and preservation after reimport.
- [x] Verify colored highlights, comments, bookmarks, and account switching after restart.

## Current Verified State
- Native profile isolation now passes: synthetic user:cache-acceptance could not see guest highlights, bookmark, or draft after restart. Guest state returned after restoring the guest session and restarting. The remaining ticket closeout requires final review/commit; earlier statements below about pending profile acceptance are historical.
- Private per-device ownership, durable local outbox, legacy guest isolation, and additive production schema push are implemented.
- Real PostgreSQL retry/conflict/rollback verification passed only against the isolated localhost test cluster. No production smoke-test approval is needed or being pursued for this gate.
- Android bookmark, colored-highlight, comment, and editable-page draft cold offline reopen passed. WebView reimport returned to database page 170/source page 106 with blue highlight and bookmark preserved; all three synthetic comments remain unchanged in isolated SQL and the reader shows Comments (3). Native post-reimport recolor also passes with the same local ID, revision 4 and one active highlight. Native profile switching remains open.
- Local SQLite integration verifies content/tree replacement preserves annotations, drafts, and outbox state. This does not replace native acceptance.
- Latest focused repository/anchor/mutation tests: 21 passed, 70 assertions. Latest typecheck exited 2 on existing project diagnostics and missing Bun test types; no diagnostics in the changed annotation hook/repository or reader production modules. Scoped hook/repository lint and diff whitespace checks passed.
- Commit, push, API deployment, and Expo preview publication remain outstanding. Do not reconnect emulator test annotations to production.

## Validation Evidence
- Ticket implementation, review and recorded verification committed and pushed in b5f678f3. This ticket is complete; overall story release and remaining platform edge cases stay open in Tickets 1/4/7/8. Earlier pending statements below are historical.
- Native profile test used the existing profile store with no token change and only the isolated localhost API. Guest scope contained six annotations and the editable-page draft; the other scope initially contained neither. Reader/editor screenshots confirm isolation: /tmp/alghurobaa-profile-isolated-reader.png and /tmp/alghurobaa-profile-isolated-draft.png. Guest restoration screenshot /tmp/alghurobaa-guest-profile-restored.png confirms the purple highlight and bookmark returned. No production records were changed.
- Offline native draft recovery passed: disabled Wi-Fi/mobile data, force-stopped the app, relaunched through USB-forwarded Metro, opened cached user-source book 90003/page 900170, and entered edit mode. The rich editor restored Original local teDraft-restart-acceptancest paragraph. without a server save. Screenshot /tmp/alghurobaa-draft-offline-restored.png. Guest-owned private SQLite persistence was independently read before restart. Wi-Fi and mobile data were restored afterward. Native profile-switch behavior is not claimed by this check.
- Draft/profile review found that the hook's load-error update reused the previous state's draft while replacing its profile/page key. Added a scope-aware error-state helper: preserve a draft only for the same key, otherwise clear it while retaining a retryable load error. Two pure state tests plus SQLite repository regressions pass (23 tests, 90 assertions); scoped lint passes. This is a reviewed error-path fix, not native profile-switch acceptance.
- Native draft check started using isolated localhost book 90003/page 900170 (editable user-source fixture, not a production book). Entered Draft-restart-acceptance in the actual rich editor. Read-only Android SQLite query confirms guest-owned draft text Original local teDraft-restart-acceptancest paragraph. No server-save action was used. Offline force-stop/reopen verification is underway; Wi-Fi and mobile data are temporarily disabled and must be restored after the check.
- Native recolor-after-reimport acceptance passed after a cold restart: selected the existing word and pressed Highlight (current selected color purple). Isolated SQL now returns exactly one active highlight for book 3/page 170: highlight-1788917246255-bwkgjpo2gyr, revision 4, color #8b5cf6, new paragraph ID 1. Original identity survived paragraph replacement and no duplicate was inserted. Screenshot /tmp/alghurobaa-reimport-color-picker.png shows the applied purple paragraph rail (despite the screenshot filename, the action applied the selected color directly rather than opening a picker). Checklist 3 is now complete based on native reimport/recolor plus prior isolated retry/rollback verification.
- 2026-09-09 continued reimport acceptance using the real Shamela WebView and isolated API port 3501. Captured /book/23833/106, saved to existing book 3/page 170 (contentVersion 1), then used View Saved Page from the incomplete-tree root step. Screenshot /tmp/alghurobaa-reimport-reader-preserved.png shows the blue highlight and bookmark retained; unmatched legacy highlights stay saved with explicit feedback rather than incorrect placement. Isolated SQL confirms all three comment revisions remain 1, blue highlight revision remains 3, and previous duplicate tombstones remain deleted. Reader shows Comments (3). No production annotation writes occurred.
- Review exposed recolor-after-reimport duplication: stored paragraph IDs no longer matched the reanchored reader range. Added a failing SQLite regression, then passed current cached paragraphs into the atomic recolor operation and reused the reader's unique-quote anchor logic. Recolor now retains original identity/note; an additional ambiguous-quote regression proves old highlights are not overwritten speculatively. 21 tests/70 assertions pass; native post-reimport recolor verification remains pending.
- Resolved exact-text query discrepancy by reading only synthetic localhost book-3 comments: one new revision-1 active row contains Send-repaint-chec (the automated input omitted the final k before submission). Submission therefore did persist once, alongside the two previous synthetic comments. Do not attribute the missing final character to storage without a UI-captured input value; future typing tests must inspect the complete field before pressing Send. Repaint-after-submit visual evidence remains valid.
- Send-transition visual retest: attempted Send-repaint-check after confirming focus, tapped Send, then captured /tmp/alghurobaa-send-repaint-check.png without any intervening scroll. First paragraphs and blue/green highlights remain painted; composer/keyboard closed. However the exact-text localhost PostgreSQL query returned ZERO rows, so this attempt does not prove a successful persisted submission. Investigate input/submission timing and local outbox before claiming the end-to-end Send retest complete. No production writes or preview publication occurred. Focused reader/annotation/capture tests: 36 passed, 100 assertions.
- Repaint isolation: suspending maintainVisibleContentPosition while the composer is open prevented the previously reproduced blank paragraphs on keyboard Back dismissal, without scrolling. Screenshot /tmp/alghurobaa-keyboard-anchor-test.png shows the first highlighted paragraphs still visible. Kept the focused condition and extended it through keyboardHeight > 0 so native anchoring is not re-enabled during Send's keyboard dismissal. Normal reader prepend anchoring remains enabled outside keyboard/composer mode. Send-transition retest remains pending. Scoped lint has zero errors and existing hook warnings; reader-position/capture regressions pass.
- Keyboard-close follow-up found a separate native repaint defect: earlier reader paragraphs become blank after composer open/close but reappear after scrolling, with data/highlights intact. Reproduced after successful send and after keyboard Back dismissal. Explicit removeClippedSubviews=false did not resolve it and was reverted rather than retained speculatively. Evidence: /tmp/alghurobaa-reader-keyboard-closed-no-clip.png versus /tmp/alghurobaa-reader-after-keyboard-scroll.png. Composer visibility/send is verified, but reader keyboard-transition repaint remains a release blocker to investigate (native maintainVisibleContentPosition and paragraph overlay layout are relevant next inspection points).
- Android composer retest: core React Native height avoidance still left the lower 66 physical pixels covered. Switched the existing wrapper to KeyboardAvoidingView from the already-installed react-native-keyboard-controller (root KeyboardProvider already exists). The entire input and Send control now sit above the keyboard: /tmp/alghurobaa-composer-above-keyboard.png. Tapping Send without dismissing the keyboard created exactly one Visible-send-acceptance comment in isolated PostgreSQL. No production writes. The rich editor remains on its previous Android manual-inset path; iOS/editor regression verification remains pending. During dev-client typing attempts, sending keyboard events before confirming input focus triggered development reloads; final retest explicitly confirmed focused input before typing.
- Applied focused composer fix: existing KeyboardAvoidingView uses height avoidance on Android only while the read-mode comment input is open. iOS padding and rich-editor manual keyboard handling remain unchanged to avoid double compensation. Reader lint: zero errors, two existing warnings; 13 capture/standards tests pass. Native keyboard-open/send/close retest remains required on the new bundle.
- 2026-09-09 native comment persistence verified: screenshot approval is available again. Revalidated live API PID 22255 startup guard targeting only 127.0.0.1:55439 and Metro PID 22323/8085. Entered Cache-comment-restart-verified in page 170; isolated PostgreSQL returned exactly one active comment at revision 1 (comment-1788944864691-citkiw6jaor). Disabled Wi-Fi/data, force-stopped/reopened dev client, then reopened reader 170 after startup completed. UI hierarchy confirms Comments (1) and the exact comment text; screenshot /tmp/alghurobaa-comment-offline-reopened-verified.png. Both networks restored afterward. Native reimport/profile-switch checks remain open.
- Native comment entry exposed a keyboard overlap defect: the Android keyboard covers the composer/send button because the reader KeyboardAvoidingView has no Android behavior. Hiding the keyboard allowed the verified save. Screenshot /tmp/alghurobaa-comment-entry.png. Fix and verify before release; do not treat the successful save as keyboard-layout acceptance.
Entries below are a newest-first implementation log. Earlier statements such as "not yet implemented", "production push pending", runtime handles, and pending-test notes describe their historical stage, not the current verified state above.

- 2026-09-09 review follow-up: separated durable annotation writes from post-commit notification/refresh in commitAnnotationMutation, used by both save and remove. A refresh failure after a successful write no longer reports the write as failed or invites duplicate comment creation. Sync is scheduled before refresh. Three new regression tests cover committed-write/failed-refresh, actual disk-write failure, and successful ordering; reproduced the initial missing helper failure before implementation. Scoped hook/helper lint passes without warnings.
- Added a real SQLite integration regression replacing page content and chapter hierarchy in the same cache that stores private annotations/drafts. Reopening the annotation repository preserves both comment and colored highlight payloads, identities/revisions, pending outbox, and private draft; another profile sees no records. Rendering reanchors both to the new paragraph ID without mutating their stored data. Combined repository/mutation run: 15 tests, 52 assertions passed. This is local integration evidence, not native restart/reimport acceptance; checklist completion remains 50%.
- 2026-09-09 continuation: comment submission now has a synchronous in-flight guard, disabled input/send controls with a loading indicator, visible failure feedback retaining typed text, and submission-token plus route checks preventing late completion from clearing another page's form. Route change/unmount invalidates the token. Reader lint: zero errors, two existing dependency warnings. Focused annotation/anchor tests: 15 passed, 55 assertions. Broader book regression: 149 passed, 543 assertions across 25 files. Expo typecheck remains exit 2 with project/module/test-type diagnostics; not a clean typecheck.
- Emulator accepted the synthetic comment Cache-comment-restart-20260909 on page 170 and the form closed. Server-row verification and offline restart have NOT been confirmed. Screenshot approval was rejected because automatic approval review hit a usage limit; do not bypass this rejection. No screenshot or local SQL confirmation was produced by that rejected call. Keep emulator test annotations isolated from production. Release remains uncommitted/unpushed/unpublished; remaining acceptance and review gates are still open.
- 2026-09-09 native atomic-recolor retest passed on the refreshed bundle: repeated color/Highlight actions changed the selected passage to blue #38bdf8. The isolated PostgreSQL query returned exactly one active row for page 170, reusing highlight-1788917246255-bwkgjpo2gyr at revision 3; the prior duplicate is no longer active. After disabling Wi-Fi/data, force-stopping and relaunching the dev client, page 106 visibly retained the blue range and border. Screenshot: /tmp/alghurobaa-blue-highlight-offline-reopened.png. Existing green highlighting was also retained. Both networks restored afterward. Unrelated development LogBox network warnings remain visible; comment/reimport/profile acceptance remains pending.
- Runtime recovery: API 81027 no longer existed and port 3501 had no listener; restarted isolated-only API as session 20669. Metro 8085 had no listener and was restarted as session 34029; observed fresh 8,595-module bundle and localhost API target. PostgreSQL PID 76200 verified live. Physical Android R5CX153J81W is also connected but all these tests explicitly used emulator-5556. Repository/anchor rerun: 15 tests passed, 55 assertions. Do not reconnect emulator annotation test data to production.
- Atomic recoloring implemented in the private SQLite repository. Highlight saves discover overlapping ranges in the same scope/book/page transaction, reuse the first existing identity and note, and tombstone duplicate ranges before committing the replacement. The reader no longer performs separate delete/create calls. Failed writes roll back the entire replacement. Test injects disk-full after duplicate tombstoning and proves original rows remain; successful and repeated recolors collapse to one identity with its note intact. 11 annotation repository tests passed, 40 assertions. Native retest remains pending.
- Scoped lint reports no errors; removed the now-unused derived highlight list. Existing hook dependency warnings remain. Expo typecheck still fails on project/test-type diagnostics, with no changed-production-module errors in this run; sessions 50565 and 76664 are terminal. git diff --check passed. Isolated API 81027 remains the intended target; do not reconnect native test annotations to production.
- 2026-09-09 native isolation setup: production-backed local API session 7654 was stopped (exit 130). Current API session 81027 serves port 3501 using only local PostgreSQL 127.0.0.1:55439/public, guarded at startup. Metro still targets 3501; do not restore a production-backed API while unsynced native test changes remain. Seeded book 3/page 170/eight paragraphs from the content-only exported JSON using /tmp/alghurobaa-seed-native-annotations.ts. No private data was copied from production. Cluster PID 76200 was verified live with escalated pg_ctl status.
- Native color test selected the first paragraph's existing passage and chose orange #f97316. Actual isolated-server rows prove native sync reached book 3/page 170, paragraph 104/pid 1, offsets 9-18. Rapid color + Highlight actions reproduced TWO identical new orange highlights plus the tombstone for the former purple legacy highlight. These local-test changes currently remain on the emulator/local server; do not call the acceptance complete or silently reconnect them to production.
- Added an in-flight guard to prevent overlapping reader highlight saves, route-safe completion UI updates, and visible retry feedback on failure. Reader ESLint: zero errors, two pre-existing hook dependency warnings; git diff --check passed. Native retest and offline color reopen are next. Further review must make overlapping-highlight replacement atomic: current delete-then-create can lose the old highlight if creating its replacement fails. Fix this before claiming preservation under storage errors.
- 2026-09-09 real PostgreSQL integration is now verified without production writes. Created an isolated PostgreSQL 16 cluster at /tmp/alghurobaa-book-cache-pg.8FEt1a/data, listening only on 127.0.0.1:55439, user book_cache_test, database postgres. Applied the project schema with both POSTGRES_URL and DIRECT_URL explicitly targeting localhost, then seeded a synthetic source-23833 book/page (app book ID 1). Cluster is retained for further isolated device-sync verification; use pg_ctl with that exact data directory to inspect/stop it.
- The gated PostgreSQL test now rejects non-local URLs. Two tests passed, seven assertions: retry deduplication, tombstones resisting stale replay, private owner isolation, rollback cleanup, and mixed-batch conflict rollback using the route's real transaction. The second test commits one synthetic row, rejects a batch containing a new row plus a conflicting revision, verifies only the original remains unchanged, then deletes that test-owner row. No production smoke test was run or bypassed; production permission is no longer needed to satisfy this SQL integration gate.
- Reproduction: from apps/api, set POSTGRES_URL=postgresql://book_cache_test@127.0.0.1:55439/postgres, POSTGRES_SCHEMA=public and BOOK_ANNOTATION_POSTGRES_TEST=1, then run bun test src/trpc/routers/book-annotation.postgres.test.ts against the existing isolated fixture. Native color/comment/reimport acceptance is still pending; do not mark checklist 3 complete from SQL evidence alone.
- 2026-09-09 offline saved-items follow-up: local rows load before network checks; known-offline/unknown connectivity skips remote refresh, reconnect restarts the focused load, and summary requests now use the bounded deadline with cancellation on blur/scope change. Android cold offline restart retained the bookmark preview without the previous page-details refresh error. Screenshot: /tmp/alghurobaa-bookmarks-offline-no-refresh-error.png. Unrelated development network LogBox notifications remain outside this fix. Both network connections restored afterward.
- Broader regression after this change: 147 tests passed, 541 assertions across 26 files. Scoped saved-items ESLint has zero errors and an existing refresh-dependency warning. Expo typecheck remains exit 2 on project diagnostics with no saved-items production diagnostic; session 58023 is terminal. git diff --check passed. Native reconnect behavior and complete annotation acceptance remain pending.
- 2026-09-09 Android bookmark acceptance: page 106 initially exposed Add bookmark, was saved through the reader, and appeared under the top-right menu's Bookmarks list as Page 83 (Shamela 106) with its opening Arabic text. After disabling both networks and force-stopping/relaunching the development client, the list retained one bookmark and its preview. Tapping it reopened cached page 106; accessibility reported Remove bookmark and selected=true. Both network connections were restored. The local test bookmark remains saved. Screenshot: /tmp/alghurobaa-bookmark-offline-reopened.png. Offline metadata refresh displays a nonblocking warning; colored highlights/comments and profile-switch native acceptance remain pending.
- Added localized bookmark action labels and selected accessibility state. Bookmark/store tests: 5 passed, 20 assertions. Reader ESLint has no errors and two existing hook-dependency warnings (reloadHighlights/pageDocument). Expo typecheck still exits 2 on project diagnostics, with no reader-screen diagnostic; session 73168 is terminal. Metro 50263 was polled and remains live.
- 2026-09-09: closed the separate Prisma logging path in the shared DB client. Error/warning logs now emit events handled with fixed severity-only diagnostics; raw messages, targets, query arguments and stacks are never passed to the console callbacks. This applies to all shared-client queries because Prisma events do not safely identify request ownership. Request-level logging remains unchanged except the previously added annotation redaction.
- Actual Prisma validation regression uses a dummy localhost adapter and an invalid unique lookup that fails before database access. It verifies a diagnostic event is emitted without the private sentinel. Combined DB logging, API redaction and annotation protocol suite: 11 tests passed, 36 assertions. DB package typecheck passed after correcting NodeNext import extensions. No schema changes or production writes were needed; existing production SQL smoke-test approval remains outstanding. Long-lived API processes need restart/deployment to use the new logger.
- Privacy review fixed API error logging for bookAnnotation routes: request inputs, query strings, and exception messages/stacks are redacted because validation/database errors can contain private text. Mixed-batch and unresolved-route URLs are also handled. Nine focused logging/protocol tests passed (30 assertions). This does not claim PostgreSQL write-test approval or native annotation-sync acceptance.
- Added non-destructive rendering-time annotation reanchoring. Existing paragraph identity plus validated text offsets or a unique exact passage match can position a highlight after reimport; ambiguous/missing matches retain the original annotation and show a saved-but-unmatched notice. Colors, notes, comments, revisions, and outbox state are unchanged.
- Entire reader windows now use private local annotations, not legacy shared-user annotations from neighboring-page API responses. Four focused anchor tests pass for changed IDs, repeated quotes, missing text, and retained page-level comments.
- Legacy page drafts lazily recover only into guest-scoped private storage, preserving HTML/text/base version/timestamps and the original table. Durable per-page markers prevent cleared or superseded drafts from reappearing. Repository regression confirms restart and no cross-profile recovery.
- Latest focused run: 22 tests passed across cache repository and annotation anchors (91 assertions). Android reimport/highlight verification and the gated real PostgreSQL write test remain outstanding; checklist 3 is not marked complete from unit evidence alone.
- Native reader and saved-item hooks now use the private repository and capability-gated coordinator. Foreground/reconnect recovery rotates pending books; app background/profile cleanup cancels active work. Reader displays local-retention feedback and a retry action on sync failure.
- Legacy highlights/comments are copied once into the device guest scope without automatic upload, preserving the original tables. Private deletion tombstones prevent reimport resurrection; non-guest profiles never inherit these records. New edits use the private outbox.
- Production schema drift was inspected and contained only BookDeviceAnnotation plus its index. `TURBO_UI=true bun db:push` completed successfully against production (Prisma reported schema in sync, 6.27s). The initial non-TUI invocation failed before database work because Turbo required terminal UI.
- A gated rollback-only PostgreSQL smoke test was added, but execution was rejected by safety review because the user had not explicitly authorized production test writes. User approval was requested; no production smoke-test writes occurred. This does not block local/device work. Real SQL retry/tombstone verification remains pending.
- Latest focused run: 16 private API/local repository tests passed (58 assertions), including once-only legacy adoption, no implicit legacy upload, and profile-safe pending-book discovery. Native TypeScript checks showed no diagnostics in changed production modules; existing project/test-type errors remain.
- Added a shared device-annotation validation contract, private local SQLite annotation/outbox repository, and capability-gated sync coordinator. Local revisions and tombstones survive restart; stale acknowledgements/failure reports cannot clear newer edits. Cache eviction protects private annotations and unsent deletions.
- Latest focused run: 15 tests passed across the local annotation repository/coordinator and private API (51 assertions). Real SQLite tests cover lost responses, replay, cancellation after receipt, unsupported-server no-upload behavior, transactional identity rejection, and outbox limits. Cache repository regressions also passed after adding annotation eviction protection.
- Remaining integration: switch native reader/saved-item hooks to the new repository, adopt legacy records without arbitrary account attribution, wire foreground retry/status UI, preserve/reanchor annotations on reimport, verify real PostgreSQL and Android behavior, and perform the required production schema push/release sequence. The new coordinator is not yet invoked by native hooks.
- Added the private bookAnnotation protocol (capabilities/list/sync) and BookDeviceAnnotation server model. Ownership uses the existing hashed installation capability plus local profile scope, never shared user 1. Batches are limited to 20, use one parameterized upsert, retain deletion tombstones, and reject reused revisions or moved identities atomically.
- Six API contract tests pass (24 assertions): capability rejection, owner/scope isolation, bounded batch writes, duplicate retry handling, stale-create deletion protection, conflict rollback, and paginated deletion pulls. The test database is a transactional mock; actual PostgreSQL execution still needs verification.
- Legacy shared-user pull/upload functions are now disconnected in mobile hooks. Guest changes remain in local SQLite while the private client protocol is integrated. This is an interim safe state, not completion of private sync.
- Prisma client generation succeeded after adding BookDeviceAnnotation. Production schema push is now required before deploying the dependent API; it has not been run yet. Mobile private outbox, legacy annotation adoption, and reimport anchor handling remain incomplete.
- User selected private per-device sync, not a new sign-in/account system. Keep guest annotations local until the private sync path is supported; never continue shared-default-user sync as a fallback. Implement and test device capability ownership and retry idempotency before enabling uploads.
- Bookmark/reading-position/preview stores are separated by local profile. Existing device-owned records remain in the original guest store. Async summary updates capture their originating store action rather than resolving a potentially changed profile after the request.
- Three focused store tests pass, covering restart, profile isolation, legacy guest retention, duplicate bookmarks, and queued startup updates that wait for hydration instead of overwriting existing bookmarks.
- New drafts use private SQLite rows keyed by account scope and page ID. Existing unscoped drafts remain untouched for explicit legacy recovery; they are not silently attributed to the current user.
- Draft tests verify account separation, restart persistence, stale-write rejection, compare-and-delete protection, and eviction protection. Drafts never enter the portable export queue.
- Remaining critical server work: getHighlightsForBook/getCommentsForBook and bulk sync currently use getDefaultBookUserId(), and sync creates are not idempotent. Local scoping alone does not complete account isolation; authenticated server ownership and retry deduplication still require implementation and tests.
- Focused cache format, file recovery, SQLite, local-first resource, page mapping, resolver, and download tests have passed during implementation.
- Existing page-loader regression tests have passed.
- Package typechecking retains pre-existing diagnostics and missing Bun test types; no new production-file diagnostics were observed in the last scoped review.
- No Android acceptance, final review, commit, deployment, or preview verification is claimed for this feature yet.
