diff --git a/android/proguard-rules.txt b/android/proguard-rules.txt
index 1b5bf6f9bba6b898e686c141e23e8048cb8332ec..14148dd2146ac820e3cae86c25a52234cdba55ce 100644
--- a/android/proguard-rules.txt
+++ b/android/proguard-rules.txt
@@ -1,3 +1,14 @@
 
 # Ignore "not found" errors for libraries
 -dontwarn com.google.android.exoplayer2.**
+-keepclassmembers class com.doublesymmetry.kotlinaudio.notification.NotificationManager {
+    private com.google.android.exoplayer2.ext.mediasession.MediaSessionConnector mediaSessionConnector;
+    private com.google.android.exoplayer2.ui.PlayerNotificationManager internalNotificationManager;
+}
+-keepclassmembers class com.google.android.exoplayer2.ui.PlayerNotificationManager {
+    private com.google.android.exoplayer2.ui.PlayerNotificationManager$CustomActionReceiver customActionReceiver;
+    private java.util.Map customActions;
+    private android.content.IntentFilter intentFilter;
+    private int instanceId;
+    private com.google.android.exoplayer2.Player player;
+}
diff --git a/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicEvents.kt b/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicEvents.kt
index 9a2ee90172ab9800fd5196ef8f9281b9ee981fbb..141134a38c098ae5def6d6352fb88db674a652f7 100644
--- a/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicEvents.kt
+++ b/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicEvents.kt
@@ -34,6 +34,7 @@ class MusicEvents(private val reactContext: ReactContext) : BroadcastReceiver()
         const val BUTTON_SET_RATING = "remote-set-rating"
         const val BUTTON_JUMP_FORWARD = "remote-jump-forward"
         const val BUTTON_JUMP_BACKWARD = "remote-jump-backward"
+        const val BUTTON_CUSTOM_ACTION = "remote-custom-action"
         const val BUTTON_DUCK = "remote-duck"
 
         // Playback Events
diff --git a/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicModule.kt b/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicModule.kt
index b2409a09939164c49c0f7a16bb6d3284e8eab8fb..20e2843318e098963dde2fdce40ec2986dec0ff9 100644
--- a/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicModule.kt
+++ b/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicModule.kt
@@ -38,6 +38,10 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     private lateinit var musicService: MusicService
     private val context = reactContext
 
+    private fun launch(block: suspend () -> Unit) {
+        scope.launch { block() }
+    }
+
     @Nonnull
     override fun getName(): String {
         return "TrackPlayerModule"
@@ -251,7 +255,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun updateOptions(data: ReadableMap?, callback: Promise) = scope.launch {
+    fun updateOptions(data: ReadableMap?, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         val options = Arguments.toBundle(data)
@@ -264,7 +268,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun add(data: ReadableArray?, insertBeforeIndex: Int, callback: Promise) = scope.launch {
+    fun add(data: ReadableArray?, insertBeforeIndex: Int, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         try {
@@ -285,7 +289,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun load(data: ReadableMap?, callback: Promise) = scope.launch {
+    fun load(data: ReadableMap?, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
         if (data == null) {
             callback.resolve(null)
@@ -301,14 +305,14 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun move(fromIndex: Int, toIndex: Int, callback: Promise) = scope.launch {
+    fun move(fromIndex: Int, toIndex: Int, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
         musicService.move(fromIndex, toIndex)
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun remove(data: ReadableArray?, callback: Promise) = scope.launch {
+    fun remove(data: ReadableArray?, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
         val inputIndexes = Arguments.toList(data)
         if (inputIndexes != null) {
@@ -332,7 +336,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
 
     @ReactMethod
     fun updateMetadataForTrack(index: Int, map: ReadableMap?, callback: Promise) =
-        scope.launch {
+        launch {
             if (verifyServiceBoundOrReject(callback)) return@launch
 
             if (index < 0 || index >= musicService.tracks.size) {
@@ -348,7 +352,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
         }
 
     @ReactMethod
-    fun updateNowPlayingMetadata(map: ReadableMap?, callback: Promise) = scope.launch {
+    fun updateNowPlayingMetadata(map: ReadableMap?, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         if (musicService.tracks.isEmpty())
@@ -364,7 +368,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun clearNowPlayingMetadata(callback: Promise) = scope.launch {
+    fun clearNowPlayingMetadata(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         if (musicService.tracks.isEmpty())
@@ -375,7 +379,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun removeUpcomingTracks(callback: Promise) = scope.launch {
+    fun removeUpcomingTracks(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.removeUpcomingTracks()
@@ -383,7 +387,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun skip(index: Int, initialTime: Float, callback: Promise) = scope.launch {
+    fun skip(index: Int, initialTime: Float, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.skip(index)
@@ -396,7 +400,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun skipToNext(initialTime: Float, callback: Promise) = scope.launch {
+    fun skipToNext(initialTime: Float, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.skipToNext()
@@ -409,7 +413,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun skipToPrevious(initialTime: Float, callback: Promise) = scope.launch {
+    fun skipToPrevious(initialTime: Float, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.skipToPrevious()
@@ -422,7 +426,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun reset(callback: Promise) = scope.launch {
+    fun reset(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.stop()
@@ -433,7 +437,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun play(callback: Promise) = scope.launch {
+    fun play(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.play()
@@ -441,7 +445,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun pause(callback: Promise) = scope.launch {
+    fun pause(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.pause()
@@ -449,7 +453,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun stop(callback: Promise) = scope.launch {
+    fun stop(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.stop()
@@ -457,7 +461,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun seekTo(seconds: Float, callback: Promise) = scope.launch {
+    fun seekTo(seconds: Float, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.seekTo(seconds)
@@ -465,7 +469,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun seekBy(offset: Float, callback: Promise) = scope.launch {
+    fun seekBy(offset: Float, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.seekBy(offset)
@@ -473,7 +477,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun retry(callback: Promise) = scope.launch {
+    fun retry(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.retry()
@@ -481,7 +485,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun setVolume(volume: Float, callback: Promise) = scope.launch {
+    fun setVolume(volume: Float, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.setVolume(volume)
@@ -489,14 +493,14 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun getVolume(callback: Promise) = scope.launch {
+    fun getVolume(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         callback.resolve(musicService.getVolume())
     }
 
     @ReactMethod
-    fun setRate(rate: Float, callback: Promise) = scope.launch {
+    fun setRate(rate: Float, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.setRate(rate)
@@ -504,14 +508,14 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun getRate(callback: Promise) = scope.launch {
+    fun getRate(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         callback.resolve(musicService.getRate())
     }
 
     @ReactMethod
-    fun setRepeatMode(mode: Int, callback: Promise) = scope.launch {
+    fun setRepeatMode(mode: Int, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.setRepeatMode(RepeatMode.fromOrdinal(mode))
@@ -519,14 +523,14 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun getRepeatMode(callback: Promise) = scope.launch {
+    fun getRepeatMode(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         callback.resolve(musicService.getRepeatMode().ordinal)
     }
 
     @ReactMethod
-    fun setPlayWhenReady(playWhenReady: Boolean, callback: Promise) = scope.launch {
+    fun setPlayWhenReady(playWhenReady: Boolean, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         musicService.playWhenReady = playWhenReady
@@ -534,32 +538,33 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun getPlayWhenReady(callback: Promise) = scope.launch {
+    fun getPlayWhenReady(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         callback.resolve(musicService.playWhenReady)
     }
 
     @ReactMethod
-    fun getTrack(index: Int, callback: Promise) = scope.launch {
+    fun getTrack(index: Int, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         if (index >= 0 && index < musicService.tracks.size) {
-            callback.resolve(Arguments.fromBundle(musicService.tracks[index].originalItem))
+            val originalItem = musicService.tracks[index].originalItem
+            callback.resolve(if (originalItem == null) null else Arguments.fromBundle(originalItem))
         } else {
             callback.resolve(null)
         }
     }
 
     @ReactMethod
-    fun getQueue(callback: Promise) = scope.launch {
+    fun getQueue(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         callback.resolve(Arguments.fromList(musicService.tracks.map { it.originalItem }))
     }
 
     @ReactMethod
-    fun setQueue(data: ReadableArray?, callback: Promise) = scope.launch {
+    fun setQueue(data: ReadableArray?, callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         try {
@@ -572,7 +577,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun getActiveTrackIndex(callback: Promise) = scope.launch {
+    fun getActiveTrackIndex(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
         callback.resolve(
             if (musicService.tracks.isEmpty()) null else musicService.getCurrentTrackIndex()
@@ -580,39 +585,41 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun getActiveTrack(callback: Promise) = scope.launch {
+    fun getActiveTrack(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
-        callback.resolve(
+        val originalItem =
             if (musicService.tracks.isEmpty()) null
-            else Arguments.fromBundle(
-                musicService.tracks[musicService.getCurrentTrackIndex()].originalItem
-            )
+            else musicService.tracks[musicService.getCurrentTrackIndex()].originalItem
+
+        callback.resolve(
+            if (originalItem == null) null
+            else Arguments.fromBundle(originalItem)
         )
     }
 
     @ReactMethod
-    fun getDuration(callback: Promise) = scope.launch {
+    fun getDuration(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         callback.resolve(musicService.getDurationInSeconds())
     }
 
     @ReactMethod
-    fun getBufferedPosition(callback: Promise) = scope.launch {
+    fun getBufferedPosition(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         callback.resolve(musicService.getBufferedPositionInSeconds())
     }
 
     @ReactMethod
-    fun getPosition(callback: Promise) = scope.launch {
+    fun getPosition(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
 
         callback.resolve(musicService.getPositionInSeconds())
     }
 
     @ReactMethod
-    fun getProgress(callback: Promise) = scope.launch {
+    fun getProgress(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
         var bundle = Bundle()
         bundle.putDouble("duration", musicService.getDurationInSeconds());
@@ -622,7 +629,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun getPlaybackState(callback: Promise) = scope.launch {
+    fun getPlaybackState(callback: Promise) = launch {
         if (verifyServiceBoundOrReject(callback)) return@launch
         callback.resolve(Arguments.fromBundle(musicService.getPlayerStateBundle(musicService.state)))
     }
diff --git a/android/src/main/java/com/doublesymmetry/trackplayer/service/MusicService.kt b/android/src/main/java/com/doublesymmetry/trackplayer/service/MusicService.kt
index afa6b0f335714de0cc8741d5bbdd51b4e04bb3bb..b6f8d4fe12f775dabb22d9193e754b3a3eb33669 100644
--- a/android/src/main/java/com/doublesymmetry/trackplayer/service/MusicService.kt
+++ b/android/src/main/java/com/doublesymmetry/trackplayer/service/MusicService.kt
@@ -3,6 +3,7 @@ package com.doublesymmetry.trackplayer.service
 import android.app.*
 import android.content.Context
 import android.content.Intent
+import android.content.IntentFilter
 import android.content.pm.ServiceInfo
 import android.net.Uri
 import android.os.Binder
@@ -10,6 +11,8 @@ import android.os.Build
 import android.os.Bundle
 import android.os.IBinder
 import android.support.v4.media.RatingCompat
+import android.support.v4.media.session.PlaybackStateCompat
+import androidx.annotation.DrawableRes
 import androidx.annotation.MainThread
 import androidx.core.app.NotificationCompat
 import androidx.core.app.NotificationCompat.PRIORITY_LOW
@@ -33,6 +36,9 @@ import com.facebook.react.HeadlessJsTaskService
 import com.facebook.react.bridge.Arguments
 import com.facebook.react.jstasks.HeadlessJsTaskConfig
 import com.facebook.react.modules.core.DeviceEventManagerModule
+import com.google.android.exoplayer2.Player
+import com.google.android.exoplayer2.ext.mediasession.MediaSessionConnector
+import com.google.android.exoplayer2.ui.PlayerNotificationManager
 import com.google.android.exoplayer2.ui.R as ExoPlayerR
 import kotlinx.coroutines.*
 import kotlinx.coroutines.flow.flow
@@ -42,6 +48,14 @@ import timber.log.Timber
 
 @MainThread
 class MusicService : HeadlessJsTaskService() {
+    private data class AndroidCustomAction(
+        val id: String,
+        val title: String,
+        @DrawableRes val icon: Int,
+        val isCompact: Boolean,
+        val placement: String,
+    )
+
     private lateinit var player: QueuedAudioPlayer
     private val binder = MusicBinder()
     private val scope = MainScope()
@@ -92,6 +106,9 @@ class MusicService : HeadlessJsTaskService() {
     private var capabilities: List<Capability> = emptyList()
     private var notificationCapabilities: List<Capability> = emptyList()
     private var compactCapabilities: List<Capability> = emptyList()
+    private var androidCustomActions: List<AndroidCustomAction> = emptyList()
+    private var legacyNotificationManager: PlayerNotificationManager? = null
+    private var legacyCustomActionDelegate: PlayerNotificationManager.CustomActionReceiver? = null
 
     override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
         startTask(getTaskConfig(intent))
@@ -186,10 +203,24 @@ class MusicService : HeadlessJsTaskService() {
         capabilities = options.getIntegerArrayList("capabilities")?.map { Capability.values()[it] } ?: emptyList()
         notificationCapabilities = options.getIntegerArrayList("notificationCapabilities")?.map { Capability.values()[it] } ?: emptyList()
         compactCapabilities = options.getIntegerArrayList("compactCapabilities")?.map { Capability.values()[it] } ?: emptyList()
+        androidCustomActions = options.getParcelableArrayList<Bundle>(ANDROID_CUSTOM_ACTIONS_KEY)
+            ?.mapNotNull { action ->
+                val id = action.getString("id") ?: return@mapNotNull null
+                val title = action.getString("title") ?: id
+                val icon = BundleUtils.getIconOrNull(this, action, "icon") ?: return@mapNotNull null
+                AndroidCustomAction(
+                    id = id,
+                    title = title,
+                    icon = icon,
+                    isCompact = action.getBoolean("isCompact", false),
+                    placement = action.getString("placement") ?: CUSTOM_ACTION_PLACEMENT_END,
+                )
+            }
+            ?: emptyList()
 
         if (notificationCapabilities.isEmpty()) notificationCapabilities = capabilities
 
-        val buttonsList = notificationCapabilities.mapNotNull {
+        val capabilityButtons = notificationCapabilities.mapNotNull {
             when (it) {
                 Capability.PLAY, Capability.PAUSE -> {
                     val playIcon = BundleUtils.getIconOrNull(this, options, "playIcon")
@@ -222,7 +253,6 @@ class MusicService : HeadlessJsTaskService() {
                 else -> { null }
             }
         }
-
         val openAppIntent = packageManager.getLaunchIntentForPackage(packageName)?.apply {
             flags = Intent.FLAG_ACTIVITY_CLEAR_TOP or Intent.FLAG_ACTIVITY_SINGLE_TOP
             // Add the Uri data so apps can identify that it was a notification click
@@ -233,9 +263,22 @@ class MusicService : HeadlessJsTaskService() {
         val accentColor = BundleUtils.getIntOrNull(options, "color")
         val smallIcon = BundleUtils.getIconOrNull(this, options, "icon")
         val pendingIntent = PendingIntent.getActivity(this, 0, openAppIntent, getPendingIntentFlags())
-        val notificationConfig = NotificationConfig(buttonsList, accentColor, smallIcon, pendingIntent)
-
-        player.notificationManager.createNotification(notificationConfig)
+        val notificationConfig = NotificationConfig(capabilityButtons, accentColor, smallIcon, pendingIntent)
+
+        val notificationJob = player.notificationManager.createNotification(notificationConfig)
+        notificationJob.invokeOnCompletion { error ->
+            if (error == null) {
+                scope.launch {
+                    if (Build.VERSION.SDK_INT >= 33) {
+                        configureAndroidCustomActions(options)
+                    } else {
+                        configureLegacyCustomActions()
+                    }
+                }
+            } else {
+                Timber.e(error, "Failed to create the media notification")
+            }
+        }
 
         // setup progress update events if configured
         progressUpdateJob?.cancel()
@@ -247,6 +290,228 @@ class MusicService : HeadlessJsTaskService() {
         }
     }
 
+    @MainThread
+    private fun configureAndroidCustomActions(options: Bundle) {
+        if (androidCustomActions.isEmpty()) return
+
+        val mediaSessionConnector = runCatching {
+            val field = player.notificationManager.javaClass
+                .getDeclaredField(MEDIA_SESSION_CONNECTOR_FIELD)
+            field.isAccessible = true
+            field.get(player.notificationManager) as MediaSessionConnector
+        }.getOrElse { error ->
+            Timber.e(error, "Unable to access the media session connector")
+            return
+        }
+
+        val providers = buildList {
+            if (notificationCapabilities.contains(Capability.JUMP_BACKWARD)) {
+                add(
+                    createMediaSessionAction(
+                        id = JUMP_BACKWARD_ACTION_ID,
+                        title = "Back 5 seconds",
+                        icon = BundleUtils.getIcon(
+                            this@MusicService,
+                            options,
+                            "rewindIcon",
+                            TrackPlayerR.drawable.rewind,
+                        ),
+                        onAction = ::emitJumpBackward,
+                    )
+                )
+            }
+            if (notificationCapabilities.contains(Capability.JUMP_FORWARD)) {
+                add(
+                    createMediaSessionAction(
+                        id = JUMP_FORWARD_ACTION_ID,
+                        title = "Forward 5 seconds",
+                        icon = BundleUtils.getIcon(
+                            this@MusicService,
+                            options,
+                            "forwardIcon",
+                            TrackPlayerR.drawable.forward,
+                        ),
+                        onAction = ::emitJumpForward,
+                    )
+                )
+            }
+            androidCustomActions.forEach { action ->
+                add(
+                    createMediaSessionAction(
+                        id = action.id,
+                        title = action.title,
+                        icon = action.icon,
+                        onAction = { emitCustomAction(action.id) },
+                    )
+                )
+            }
+        }
+
+        mediaSessionConnector.setCustomActionProviders(*providers.toTypedArray())
+    }
+
+    @MainThread
+    private fun configureLegacyCustomActions() {
+        if (androidCustomActions.isEmpty()) return
+
+        runCatching {
+            val kotlinAudioNotificationManager = player.notificationManager
+            val internalManagerField = kotlinAudioNotificationManager.javaClass
+                .getDeclaredField(INTERNAL_NOTIFICATION_MANAGER_FIELD)
+                .apply { isAccessible = true }
+            val notificationManager = internalManagerField.get(
+                kotlinAudioNotificationManager
+            ) as PlayerNotificationManager
+
+            val receiverField = PlayerNotificationManager::class.java
+                .getDeclaredField(CUSTOM_ACTION_RECEIVER_FIELD)
+                .apply { isAccessible = true }
+            if (legacyNotificationManager !== notificationManager) {
+                legacyNotificationManager = notificationManager
+                legacyCustomActionDelegate = receiverField.get(
+                    notificationManager
+                ) as? PlayerNotificationManager.CustomActionReceiver
+            }
+            val delegate = legacyCustomActionDelegate
+            val receiver = createLegacyCustomActionReceiver(delegate)
+
+            val playerField = PlayerNotificationManager::class.java
+                .getDeclaredField(PLAYER_FIELD)
+                .apply { isAccessible = true }
+            val attachedPlayer = playerField.get(notificationManager) as? Player
+            notificationManager.setPlayer(null)
+
+            try {
+                receiverField.set(notificationManager, receiver)
+
+                val instanceIdField = PlayerNotificationManager::class.java
+                    .getDeclaredField(INSTANCE_ID_FIELD)
+                    .apply { isAccessible = true }
+                val instanceId = instanceIdField.getInt(notificationManager)
+                val customActionsField = PlayerNotificationManager::class.java
+                    .getDeclaredField(CUSTOM_ACTIONS_FIELD)
+                    .apply { isAccessible = true }
+                customActionsField.set(
+                    notificationManager,
+                    receiver.createCustomActions(this@MusicService, instanceId),
+                )
+
+                val intentFilterField = PlayerNotificationManager::class.java
+                    .getDeclaredField(INTENT_FILTER_FIELD)
+                    .apply { isAccessible = true }
+                val intentFilter = intentFilterField.get(notificationManager) as IntentFilter
+                androidCustomActions.forEach { intentFilter.addAction(it.id) }
+            } finally {
+                notificationManager.setPlayer(attachedPlayer)
+            }
+            notificationManager.invalidate()
+        }.onFailure { error ->
+            Timber.e(error, "Unable to install legacy media notification custom actions")
+        }
+    }
+
+    private fun createLegacyCustomActionReceiver(
+        delegate: PlayerNotificationManager.CustomActionReceiver?,
+    ): PlayerNotificationManager.CustomActionReceiver {
+        return object : PlayerNotificationManager.CustomActionReceiver {
+            override fun createCustomActions(
+                context: Context,
+                instanceId: Int,
+            ): MutableMap<String, NotificationCompat.Action> {
+                val actions = linkedMapOf<String, NotificationCompat.Action>()
+                delegate?.createCustomActions(context, instanceId)?.let(actions::putAll)
+                androidCustomActions.forEachIndexed { index, action ->
+                    val intent = Intent(action.id)
+                        .setPackage(context.packageName)
+                        .putExtra(PlayerNotificationManager.EXTRA_INSTANCE_ID, instanceId)
+                    val requestCode = instanceId * 31 + index
+                    val pendingIntent = PendingIntent.getBroadcast(
+                        context,
+                        requestCode,
+                        intent,
+                        getPendingIntentFlags(),
+                    )
+                    actions[action.id] = NotificationCompat.Action.Builder(
+                        action.icon,
+                        action.title,
+                        pendingIntent,
+                    ).build()
+                }
+                return actions
+            }
+
+            override fun getCustomActions(player: Player): MutableList<String> {
+                val delegateActions = delegate?.getCustomActions(player).orEmpty()
+                val leadingActions = androidCustomActions
+                    .filter { it.placement == CUSTOM_ACTION_PLACEMENT_START }
+                    .map { it.id }
+                val trailingActions = androidCustomActions
+                    .filter { it.placement != CUSTOM_ACTION_PLACEMENT_START }
+                    .map { it.id }
+                return (leadingActions + delegateActions + trailingActions)
+                    .distinct()
+                    .toMutableList()
+            }
+
+            override fun onCustomAction(player: Player, action: String, intent: Intent) {
+                if (androidCustomActions.any { it.id == action }) {
+                    scope.launch { emitCustomAction(action) }
+                } else {
+                    delegate?.onCustomAction(player, action, intent)
+                }
+            }
+        }
+    }
+
+    private fun createMediaSessionAction(
+        id: String,
+        title: String,
+        @DrawableRes icon: Int,
+        onAction: () -> Unit,
+    ): MediaSessionConnector.CustomActionProvider {
+        return object : MediaSessionConnector.CustomActionProvider {
+            override fun getCustomAction(player: Player): PlaybackStateCompat.CustomAction {
+                return PlaybackStateCompat.CustomAction.Builder(id, title, icon).build()
+            }
+
+            override fun onCustomAction(player: Player, action: String, extras: Bundle?) {
+                scope.launch { onAction() }
+            }
+        }
+    }
+
+    @MainThread
+    private fun emitJumpBackward() {
+        Bundle().apply {
+            val interval = latestOptions?.getDouble(
+                BACKWARD_JUMP_INTERVAL_KEY,
+                DEFAULT_JUMP_INTERVAL,
+            ) ?: DEFAULT_JUMP_INTERVAL
+            putInt("interval", interval.toInt())
+            emit(MusicEvents.BUTTON_JUMP_BACKWARD, this)
+        }
+    }
+
+    @MainThread
+    private fun emitJumpForward() {
+        Bundle().apply {
+            val interval = latestOptions?.getDouble(
+                FORWARD_JUMP_INTERVAL_KEY,
+                DEFAULT_JUMP_INTERVAL,
+            ) ?: DEFAULT_JUMP_INTERVAL
+            putInt("interval", interval.toInt())
+            emit(MusicEvents.BUTTON_JUMP_FORWARD, this)
+        }
+    }
+
+    @MainThread
+    private fun emitCustomAction(id: String) {
+        Bundle().apply {
+            putString("id", id)
+            emit(MusicEvents.BUTTON_CUSTOM_ACTION, this)
+        }
+    }
+
     @MainThread
     private fun progressUpdateEventFlow(interval: Double) = flow {
         while (true) {
@@ -837,6 +1102,7 @@ class MusicService : HeadlessJsTaskService() {
         const val MAX_CACHE_SIZE_KEY = "maxCacheSize"
 
         const val ANDROID_OPTIONS_KEY = "android"
+        const val ANDROID_CUSTOM_ACTIONS_KEY = "androidCustomActions"
 
         const val STOPPING_APP_PAUSES_PLAYBACK_KEY = "stoppingAppPausesPlayback"
         const val APP_KILLED_PLAYBACK_BEHAVIOR_KEY = "appKilledPlaybackBehavior"
@@ -850,5 +1116,17 @@ class MusicService : HeadlessJsTaskService() {
 
         const val DEFAULT_JUMP_INTERVAL = 15.0
         const val DEFAULT_STOP_FOREGROUND_GRACE_PERIOD = 5
+
+        private const val MEDIA_SESSION_CONNECTOR_FIELD = "mediaSessionConnector"
+        private const val INTERNAL_NOTIFICATION_MANAGER_FIELD = "internalNotificationManager"
+        private const val CUSTOM_ACTION_RECEIVER_FIELD = "customActionReceiver"
+        private const val CUSTOM_ACTIONS_FIELD = "customActions"
+        private const val INTENT_FILTER_FIELD = "intentFilter"
+        private const val INSTANCE_ID_FIELD = "instanceId"
+        private const val PLAYER_FIELD = "player"
+        private const val CUSTOM_ACTION_PLACEMENT_START = "start"
+        private const val CUSTOM_ACTION_PLACEMENT_END = "end"
+        private const val JUMP_BACKWARD_ACTION_ID = "jump-backward-5"
+        private const val JUMP_FORWARD_ACTION_ID = "jump-forward-5"
     }
 }
diff --git a/lib/src/constants/Event.d.ts b/lib/src/constants/Event.d.ts
index 1abe5418f6c3a143f6c7884f6a33efe343e236f9..264202e1e33b1b7a4760da9485ecc5b7434d5f48 100644
--- a/lib/src/constants/Event.d.ts
+++ b/lib/src/constants/Event.d.ts
@@ -68,6 +68,10 @@ export declare enum Event {
      * See https://rntp.dev/docs/api/events#remotejumpbackward
      **/
     RemoteJumpBackward = "remote-jump-backward",
+    /**
+     * (Android only) Fired when the user presses a custom notification action.
+     */
+    RemoteCustomAction = "remote-custom-action",
     /**
      * Fired when the user changes the position of the timeline.
      * See https://rntp.dev/docs/api/events#remoteseek
diff --git a/lib/src/constants/Event.js b/lib/src/constants/Event.js
index 9a5676d066c767b312a2958edf3bf47806a4404f..31d8def9806819de5d4a03589b445eed9a48b200 100644
--- a/lib/src/constants/Event.js
+++ b/lib/src/constants/Event.js
@@ -69,6 +69,10 @@ export var Event;
      * See https://rntp.dev/docs/api/events#remotejumpbackward
      **/
     Event["RemoteJumpBackward"] = "remote-jump-backward";
+    /**
+     * (Android only) Fired when the user presses a custom notification action.
+     */
+    Event["RemoteCustomAction"] = "remote-custom-action";
     /**
      * Fired when the user changes the position of the timeline.
      * See https://rntp.dev/docs/api/events#remoteseek
diff --git a/lib/src/interfaces/UpdateOptions.d.ts b/lib/src/interfaces/UpdateOptions.d.ts
index 7190c9018f9c332a8f20658a498f4f1419cc3101..75eccb71db7822be377a4df6d2f6715f866a28c9 100644
--- a/lib/src/interfaces/UpdateOptions.d.ts
+++ b/lib/src/interfaces/UpdateOptions.d.ts
@@ -2,6 +2,13 @@ import type { AndroidOptions } from './AndroidOptions';
 import type { FeedbackOptions } from './FeedbackOptions';
 import type { ResourceObject } from './ResourceObject';
 import type { RatingType, Capability } from '../constants';
+export interface AndroidCustomAction {
+    id: string;
+    title: string;
+    icon: ResourceObject | string;
+    isCompact?: boolean;
+    placement?: 'start' | 'end';
+}
 export interface UpdateOptions {
     android?: AndroidOptions;
     ratingType?: RatingType;
@@ -30,13 +37,14 @@ export interface UpdateOptions {
     alwaysPauseOnInterruption?: boolean;
     notificationCapabilities?: Capability[];
     compactCapabilities?: Capability[];
-    icon?: ResourceObject;
-    playIcon?: ResourceObject;
-    pauseIcon?: ResourceObject;
-    stopIcon?: ResourceObject;
-    previousIcon?: ResourceObject;
-    nextIcon?: ResourceObject;
-    rewindIcon?: ResourceObject;
-    forwardIcon?: ResourceObject;
+    androidCustomActions?: AndroidCustomAction[];
+    icon?: ResourceObject | string;
+    playIcon?: ResourceObject | string;
+    pauseIcon?: ResourceObject | string;
+    stopIcon?: ResourceObject | string;
+    previousIcon?: ResourceObject | string;
+    nextIcon?: ResourceObject | string;
+    rewindIcon?: ResourceObject | string;
+    forwardIcon?: ResourceObject | string;
     color?: number;
 }
diff --git a/lib/src/interfaces/events/EventPayloadByEvent.d.ts b/lib/src/interfaces/events/EventPayloadByEvent.d.ts
index 00082362b41e4d37d2d71f7b72393124014b2fb2..c7689105c745beb2b2c42b2b9b32b7489164d429 100644
--- a/lib/src/interfaces/events/EventPayloadByEvent.d.ts
+++ b/lib/src/interfaces/events/EventPayloadByEvent.d.ts
@@ -15,6 +15,7 @@ import type { RemoteJumpBackwardEvent } from './RemoteJumpBackwardEvent';
 import type { RemoteJumpForwardEvent } from './RemoteJumpForwardEvent';
 import type { RemotePlayIdEvent } from './RemotePlayIdEvent';
 import type { RemotePlaySearchEvent } from './RemotePlaySearchEvent';
+import type { RemoteCustomActionEvent } from './RemoteCustomActionEvent';
 import type { RemoteSeekEvent } from './RemoteSeekEvent';
 import type { RemoteSetRatingEvent } from './RemoteSetRatingEvent';
 import type { RemoteSkipEvent } from './RemoteSkipEvent';
@@ -38,6 +39,7 @@ export type EventPayloadByEvent = {
     [Event.RemotePrevious]: never;
     [Event.RemoteJumpForward]: RemoteJumpForwardEvent;
     [Event.RemoteJumpBackward]: RemoteJumpBackwardEvent;
+    [Event.RemoteCustomAction]: RemoteCustomActionEvent;
     [Event.RemoteSeek]: RemoteSeekEvent;
     [Event.RemoteSetRating]: RemoteSetRatingEvent;
     [Event.RemoteDuck]: RemoteDuckEvent;
diff --git a/lib/src/interfaces/events/RemoteCustomActionEvent.d.ts b/lib/src/interfaces/events/RemoteCustomActionEvent.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..497777ea480a8e53528b3c6c136cf9a04d2f4d60
--- /dev/null
+++ b/lib/src/interfaces/events/RemoteCustomActionEvent.d.ts
@@ -0,0 +1,3 @@
+export interface RemoteCustomActionEvent {
+    id: string;
+}
diff --git a/lib/src/interfaces/events/RemoteCustomActionEvent.js b/lib/src/interfaces/events/RemoteCustomActionEvent.js
new file mode 100644
index 0000000000000000000000000000000000000000..cb0ff5c3b541f646105198ee23ac0fc3d805023e
--- /dev/null
+++ b/lib/src/interfaces/events/RemoteCustomActionEvent.js
@@ -0,0 +1 @@
+export {};
diff --git a/lib/src/interfaces/events/index.d.ts b/lib/src/interfaces/events/index.d.ts
index 92b5df2a41eaf0d69e835d6c7f65dac011e91753..3a0f78172c87d679988f8e15763d7b7de91f6ea1 100644
--- a/lib/src/interfaces/events/index.d.ts
+++ b/lib/src/interfaces/events/index.d.ts
@@ -11,6 +11,7 @@ export * from './RemoteJumpBackwardEvent';
 export * from './RemoteJumpForwardEvent';
 export * from './RemotePlayIdEvent';
 export * from './RemotePlaySearchEvent';
+export * from './RemoteCustomActionEvent';
 export * from './RemoteSeekEvent';
 export * from './RemoteSetRatingEvent';
 export * from './RemoteSkipEvent';
diff --git a/lib/src/interfaces/events/index.js b/lib/src/interfaces/events/index.js
index 92b5df2a41eaf0d69e835d6c7f65dac011e91753..3a0f78172c87d679988f8e15763d7b7de91f6ea1 100644
--- a/lib/src/interfaces/events/index.js
+++ b/lib/src/interfaces/events/index.js
@@ -11,6 +11,7 @@ export * from './RemoteJumpBackwardEvent';
 export * from './RemoteJumpForwardEvent';
 export * from './RemotePlayIdEvent';
 export * from './RemotePlaySearchEvent';
+export * from './RemoteCustomActionEvent';
 export * from './RemoteSeekEvent';
 export * from './RemoteSetRatingEvent';
 export * from './RemoteSkipEvent';
diff --git a/lib/src/trackPlayer.js b/lib/src/trackPlayer.js
index 191e5dea7ce96f145d0a2ea16d9d925136ce4454..e8143775ff6ddc841bc56279755a124b96b39bd3 100644
--- a/lib/src/trackPlayer.js
+++ b/lib/src/trackPlayer.js
@@ -17,6 +17,9 @@ function resolveImportedAsset(id) {
         ? resolveAssetSource(id) ?? undefined
         : undefined;
 }
+function resolveNotificationIcon(icon) {
+    return typeof icon === 'string' ? { uri: icon } : resolveImportedAsset(icon);
+}
 // MARK: - General API
 /**
  * Initializes the player with the specified options.
@@ -134,14 +137,18 @@ export async function updateOptions({ alwaysPauseOnInterruption, ...options } =
             alwaysPauseOnInterruption: options.android?.alwaysPauseOnInterruption ?? alwaysPauseOnInterruption,
             ...options.android,
         },
-        icon: resolveImportedAsset(options.icon),
-        playIcon: resolveImportedAsset(options.playIcon),
-        pauseIcon: resolveImportedAsset(options.pauseIcon),
-        stopIcon: resolveImportedAsset(options.stopIcon),
-        previousIcon: resolveImportedAsset(options.previousIcon),
-        nextIcon: resolveImportedAsset(options.nextIcon),
-        rewindIcon: resolveImportedAsset(options.rewindIcon),
-        forwardIcon: resolveImportedAsset(options.forwardIcon),
+        icon: resolveNotificationIcon(options.icon),
+        playIcon: resolveNotificationIcon(options.playIcon),
+        pauseIcon: resolveNotificationIcon(options.pauseIcon),
+        stopIcon: resolveNotificationIcon(options.stopIcon),
+        previousIcon: resolveNotificationIcon(options.previousIcon),
+        nextIcon: resolveNotificationIcon(options.nextIcon),
+        rewindIcon: resolveNotificationIcon(options.rewindIcon),
+        forwardIcon: resolveNotificationIcon(options.forwardIcon),
+        androidCustomActions: options.androidCustomActions?.map((action) => ({
+            ...action,
+            icon: resolveNotificationIcon(action.icon),
+        })),
     });
 }
 /**
diff --git a/src/constants/Event.ts b/src/constants/Event.ts
index 09149463f9f00a669fb08e8110aee0fa114bdc30..ba462ecc52e07d29931b628b6c1217f1b49e6f05 100644
--- a/src/constants/Event.ts
+++ b/src/constants/Event.ts
@@ -69,6 +69,10 @@ export enum Event {
    * See https://rntp.dev/docs/api/events#remotejumpbackward
    **/
   RemoteJumpBackward = 'remote-jump-backward',
+  /**
+   * (Android only) Fired when the user presses a custom notification action.
+   */
+  RemoteCustomAction = 'remote-custom-action',
   /**
    * Fired when the user changes the position of the timeline.
    * See https://rntp.dev/docs/api/events#remoteseek
diff --git a/src/interfaces/UpdateOptions.ts b/src/interfaces/UpdateOptions.ts
index 9657caec9a817093f3240cfa3cd4a572e664e172..eff67fe07bb3088b684c26eb07a329fa267830f9 100644
--- a/src/interfaces/UpdateOptions.ts
+++ b/src/interfaces/UpdateOptions.ts
@@ -3,6 +3,14 @@ import type { FeedbackOptions } from './FeedbackOptions';
 import type { ResourceObject } from './ResourceObject';
 import type { RatingType, Capability } from '../constants';
 
+export interface AndroidCustomAction {
+  id: string;
+  title: string;
+  icon: ResourceObject | string;
+  isCompact?: boolean;
+  placement?: 'start' | 'end';
+}
+
 export interface UpdateOptions {
   android?: AndroidOptions;
   ratingType?: RatingType;
@@ -36,14 +44,15 @@ export interface UpdateOptions {
   alwaysPauseOnInterruption?: boolean;
   notificationCapabilities?: Capability[];
   compactCapabilities?: Capability[];
+  androidCustomActions?: AndroidCustomAction[];
 
-  icon?: ResourceObject;
-  playIcon?: ResourceObject;
-  pauseIcon?: ResourceObject;
-  stopIcon?: ResourceObject;
-  previousIcon?: ResourceObject;
-  nextIcon?: ResourceObject;
-  rewindIcon?: ResourceObject;
-  forwardIcon?: ResourceObject;
+  icon?: ResourceObject | string;
+  playIcon?: ResourceObject | string;
+  pauseIcon?: ResourceObject | string;
+  stopIcon?: ResourceObject | string;
+  previousIcon?: ResourceObject | string;
+  nextIcon?: ResourceObject | string;
+  rewindIcon?: ResourceObject | string;
+  forwardIcon?: ResourceObject | string;
   color?: number;
 }
diff --git a/src/interfaces/events/EventPayloadByEvent.ts b/src/interfaces/events/EventPayloadByEvent.ts
index a9d11920a69f0e3c6d358cd3aae796fb3dd1bad3..603027c61e139cadfa1695223811563f6659ec04 100644
--- a/src/interfaces/events/EventPayloadByEvent.ts
+++ b/src/interfaces/events/EventPayloadByEvent.ts
@@ -16,6 +16,7 @@ import type { RemoteJumpBackwardEvent } from './RemoteJumpBackwardEvent';
 import type { RemoteJumpForwardEvent } from './RemoteJumpForwardEvent';
 import type { RemotePlayIdEvent } from './RemotePlayIdEvent';
 import type { RemotePlaySearchEvent } from './RemotePlaySearchEvent';
+import type { RemoteCustomActionEvent } from './RemoteCustomActionEvent';
 import type { RemoteSeekEvent } from './RemoteSeekEvent';
 import type { RemoteSetRatingEvent } from './RemoteSetRatingEvent';
 import type { RemoteSkipEvent } from './RemoteSkipEvent';
@@ -40,6 +41,7 @@ export type EventPayloadByEvent = {
   [Event.RemotePrevious]: never;
   [Event.RemoteJumpForward]: RemoteJumpForwardEvent;
   [Event.RemoteJumpBackward]: RemoteJumpBackwardEvent;
+  [Event.RemoteCustomAction]: RemoteCustomActionEvent;
   [Event.RemoteSeek]: RemoteSeekEvent;
   [Event.RemoteSetRating]: RemoteSetRatingEvent;
   [Event.RemoteDuck]: RemoteDuckEvent;
diff --git a/src/interfaces/events/RemoteCustomActionEvent.ts b/src/interfaces/events/RemoteCustomActionEvent.ts
new file mode 100644
index 0000000000000000000000000000000000000000..3c86d8281923428546b8b1f08a54ab3af2c12ed8
--- /dev/null
+++ b/src/interfaces/events/RemoteCustomActionEvent.ts
@@ -0,0 +1,3 @@
+export interface RemoteCustomActionEvent {
+  id: string;
+}
diff --git a/src/interfaces/events/index.ts b/src/interfaces/events/index.ts
index 92b5df2a41eaf0d69e835d6c7f65dac011e91753..3a0f78172c87d679988f8e15763d7b7de91f6ea1 100644
--- a/src/interfaces/events/index.ts
+++ b/src/interfaces/events/index.ts
@@ -11,6 +11,7 @@ export * from './RemoteJumpBackwardEvent';
 export * from './RemoteJumpForwardEvent';
 export * from './RemotePlayIdEvent';
 export * from './RemotePlaySearchEvent';
+export * from './RemoteCustomActionEvent';
 export * from './RemoteSeekEvent';
 export * from './RemoteSetRatingEvent';
 export * from './RemoteSkipEvent';
diff --git a/src/trackPlayer.ts b/src/trackPlayer.ts
index 6f6d7234979f31e4a2fdab0fdecb891f200c0892..09a2493e78492c7371e775342a76dad535a6a569 100644
--- a/src/trackPlayer.ts
+++ b/src/trackPlayer.ts
@@ -42,6 +42,10 @@ function resolveImportedAsset(id?: number) {
     : undefined;
 }
 
+function resolveNotificationIcon(icon?: number | string) {
+  return typeof icon === 'string' ? { uri: icon } : resolveImportedAsset(icon);
+}
+
 // MARK: - General API
 
 /**
@@ -231,14 +235,18 @@ export async function updateOptions({
         options.android?.alwaysPauseOnInterruption ?? alwaysPauseOnInterruption,
       ...options.android,
     },
-    icon: resolveImportedAsset(options.icon),
-    playIcon: resolveImportedAsset(options.playIcon),
-    pauseIcon: resolveImportedAsset(options.pauseIcon),
-    stopIcon: resolveImportedAsset(options.stopIcon),
-    previousIcon: resolveImportedAsset(options.previousIcon),
-    nextIcon: resolveImportedAsset(options.nextIcon),
-    rewindIcon: resolveImportedAsset(options.rewindIcon),
-    forwardIcon: resolveImportedAsset(options.forwardIcon),
+    icon: resolveNotificationIcon(options.icon),
+    playIcon: resolveNotificationIcon(options.playIcon),
+    pauseIcon: resolveNotificationIcon(options.pauseIcon),
+    stopIcon: resolveNotificationIcon(options.stopIcon),
+    previousIcon: resolveNotificationIcon(options.previousIcon),
+    nextIcon: resolveNotificationIcon(options.nextIcon),
+    rewindIcon: resolveNotificationIcon(options.rewindIcon),
+    forwardIcon: resolveNotificationIcon(options.forwardIcon),
+    androidCustomActions: options.androidCustomActions?.map((action) => ({
+      ...action,
+      icon: resolveNotificationIcon(action.icon),
+    })),
   });
 }
 
